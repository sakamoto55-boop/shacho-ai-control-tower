# 06_リリース判定 — AI社長室MVP

> 2026-07-12

## 判定段階
1. **開発完了** … 画面・ロジック・テスト・検収記録が揃い、デモで全画面確認できる
2. **実データ検収待ち** … 開発完了に加え、社長のGoogle OAuth接続で実データ取得を確認する段階
3. **本番利用可** … 実データ検収を通過し、限定環境で1営業日運用した後

## 現在の判定：**② 実データ検収待ち**

### 根拠
- ✅ 開発完了：7領域・OAuth診断・案件台帳設定・72時間メール・期限超過・エラー境界を実装。
  `npm run build` 0エラー、`npm test` 24件全通過、スクリーンショット6点取得済み。
- ⛔ 実データ検収は未完了：社長のGoogle OAuth（4スコープ）接続がまだのため、
  Gmail/Calendar/Drive/Sheets の**実データ取得の確認が未実施**。
- したがって「③ 本番利用可」にはしない（OAuth実データ検収が前提）。

## ② → ③ に上げるための残条件
- [ ] 社長がGoogle OAuth（gmail/calendar/drive/spreadsheets readonly）に接続
  （既にGmailのみで接続済みなら「切断して再接続」）
- [ ] 当日予定・過去72時間メール・（設定時）案件台帳が実データで表示されることを確認
- [ ] 実データ状態のスクリーンショットを追加取得
- [ ] 限定環境で1営業日運用しフィードバック

## 既知の課題（詳細は 05_既知の課題.md）
- LINE WORKS はデモ固定（バックエンド必須・対象外）
- 進行中プロジェクトの実データは案件台帳シート設定が前提
- 技術構成は既存資産流用（React+Vite）を維持（Next.js等へは移行しない＝禁止事項）
