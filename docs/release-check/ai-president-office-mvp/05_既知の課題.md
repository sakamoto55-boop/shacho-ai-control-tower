# 05_既知の課題 — AI社長室MVP

> 2026-07-12

## モックのままの領域（実データ源が未定義）
- **進行中プロジェクト**：案件別粗利シート（`VITE_GOOGLE_SHEETS_PROJECT_PROFIT_ID`）が
  未設定だとデモ表示。実データ化には案件台帳シートのID設定が必要（形式は `docs/SHEETS_DATA_FORMAT.md`）。
- **期限超過タスク**：現状は受信箱（Gmail/LINE WORKS）の「期限」から超過を推定。
  専用タスクDB（Sheets等）を用意すると精度が上がる。
- **LINE WORKS**：デモ固定（バックエンド必須のため本MVP対象外）。

## 実データ表示の前提
- 社長のGoogle OAuth接続（4スコープ）が未完了の間は全領域デモ。
  以前 gmail のみで接続済みの場合は「切断→再接続」が必要（Calendar/Drive/Sheetsが取得失敗になる）。

## 仕様書との差異（意図的）
- 技術構成：仕様書推奨は Next.js/Supabase/Vercel。既存資産流用のため React+Vite+GitHub Pages を維持
  （作り替え＝別プロジェクト新規作成は禁止事項のため回避）。
- タスクの登録/更新（POST/PATCH tasks）：読み取り専用方針のため未実装。

## 次の候補
- 進行中プロジェクトの実データ源（Drive案件フォルダ or 管理シート）の確定
- 要対応メールの抽出窓を「過去72時間」に合わせる調整（現状 gmailFetcher は newer_than:1d）
- スクリーンショットの自動取得（dev/preview + ブラウザ）
