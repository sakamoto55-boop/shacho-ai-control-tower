# 04_セキュリティ確認 — AI社長室MVP（実データ検収対応）

> 2026-07-12

| 項目 | 状態 |
|------|------|
| 外部書き込み | ❌ なし（送信/返信/下書き/削除/既読化/ラベル変更/予定変更/ファイル変更/セル更新 いずれも未実装）|
| OAuthスコープ | ✅ readonly 4つのみ（gmail/calendar/drive/spreadsheets）。書き込みスコープ追加なし |
| Client Secret | ❌ 不使用（フロントに置かない）|
| アクセストークンの画面表示 | ❌ しない（診断はスコープ有無・期限のみ表示）|
| 案件台帳設定の保存 | localStorage（ID・シート名・列名のみ）。**認証情報は保存しない** |
| 秘密情報のコミット | ❌ なし（`.env` は `.gitignore`・実ID/キーのコード混入ゼロ）|
| 承認前の実行 | ❌ なし（社長確認待ちは表示のみ）|
| メール本文の全表示 | ❌ しない（件名・送信者・受信日時・要約・推奨対応のみ）|
| Gmail 72時間拡張 | ✅ 取得は GET のみ（`in:inbox newer_than:3d`）。書き込みなし |
| エラーの握りつぶし | ❌ なし（利用者向け表示＋console.errorへ分離）|
| 有料クラウドリソース | ❌ 作成なし（Cloud Run/Firebase/Supabase/DB/Webhook なし）|
| 課金設定 | ❌ なし |
| 追加devDependency | vitest / @playwright/test / @testing-library / jsdom（**開発用のみ**・本番バンドル非同梱）|

## 確認コマンド
```
git ls-files | grep -E '\.env$'                       # → なし（.env.example のみ）
git grep -nE 'sk-[A-Za-z0-9]{20}|GOCSPX-|AIza|ya29\.' # → 実キー検出なし
# 書き込みAPIの不存在（president/新規コード）
git grep -nE "method:\s*'(POST|PATCH|PUT|DELETE)'" -- client/src/core/president/ # → なし
```

## 補足
- スクリーンショットの「取得失敗」状態は、検証用の偽トークン（`FAKE_FOR_SCREENSHOT_ONLY`）を
  localStorageに注入して意図的にAPI失敗を再現したもの。実トークン・秘密情報は含まない。
