# SCREEN_LIST.md — 変更画面・確認すべき画面・想定表示内容

> 最終更新: Phase 9 — v0.9.0（2026-06-27）

---

## Phase 9 変更画面

### 1. AIコックピット（CockpitScreen.tsx）— ティール通知セクション追加

**変更内容**: LINE WORKS緊急通知セクション（ティール #F0FDFA）を BusinessData セクションの後に追加

**確認すべき箇所**:

| 箇所 | 期待する表示 |
|------|------------|
| セクション背景 | ティール #F0FDFA, ボーダー #CCFBF1 |
| セクションタイトル | 「💬 LINE WORKS通知」「デモ」「読み取り専用」バッジ |
| 件数チップ | 緊急N件, 重要AN件, 事故N件, SOSN件 |
| 緊急カード | 事故報告（赤強調）, SOS（赤強調）が最大3件 |
| riskFlag=true | 赤背景（bg-red-50）で強調表示 |
| フッター | 「デモLINE WORKS · 読み取り専用 · 送信・返信・既読化なし」 |

---

### 2. ホーム画面（Home.tsx）— ティールLINE WORKSカード追加

**変更内容**: LINE WORKSカードを BusinessData カードの後に追加

**確認すべき箇所**:

| 箇所 | 期待する表示 |
|------|------------|
| カード背景 | ティール #F0FDFA |
| カードタイトル | 「💬 LINE WORKS通知」 |
| バッジ | 「デモ」「読み取り専用」 |
| 件数チップ | 緊急N件, 重要AN件, 事故N件, SOSSN件 |
| 通知リスト | criticalの通知タイトル（最大2件） |
| リンクボタン | 「LINE WORKSの通知を確認する →」 |
| タップ動作 | 'actions' タブへ遷移 |

---

### 3. 今日の要対応（TodayActions.tsx）— LINE WORKSセクション追加

**変更内容**: LINE WORKS通知5件 + 受信箱4件のセクションを追加（返信・既読化ボタンなし）

**確認すべき箇所**:

| 箇所 | 期待する表示 |
|------|------------|
| セクションヘッダー | 「💬 LINE WORKS（デモ）」 |
| 通知バッジ | 「💬 LINE WORKS」「デモLINE WORKS」「読み取り専用」「返信未送信」「既読化なし」「社長確認待ち」 |
| 通知カード | urgency=critical → 赤ボーダー, high → オレンジボーダー |
| 受信箱カード | priority別表示（A=重要、B=通常） |
| 「返信」ボタン | 表示しない（送信禁止） |
| 「既読」ボタン | 表示しない（既読化禁止） |
| モーダル警告バナー | 「送信・返信・既読化は禁止です」 |

---

### 4. AI相談（AiChat.tsx）— LINE WORKSショートカットバー追加

**変更内容**: LINE WORKSショートカット6件のバー（ティール）を Sheets バーの上に追加

**確認すべき箇所**:

| 箇所 | 期待する表示 |
|------|------------|
| バーラベル | 「💬 LINE WORKSショートカット」（ティール #F0FDFA） |
| ショートカット1 | 「LINE WORKSの通知を要約して」 |
| ショートカット2 | 「今日のSOS・緊急連絡は？」 |
| ショートカット3 | 「事故・トラブル報告を教えて」 |
| ショートカット4 | 「欠勤・遅延情報をまとめて」 |
| ショートカット5 | 「LINE WORKSの優先度A案件は？」 |
| ショートカット6 | 「現場からの報告を整理して」 |
| 回答に含まれる文言 | 「デモLINE WORKS · 読み取り専用 · 送信・返信・既読化なし」 |

---

### 5. 設定画面（Settings.tsx）— v0.9.0 更新

**変更内容**: バージョン・フェーズ・LINE WORKS通知ステータス更新

**確認すべき箇所**:

| 箇所 | 期待する表示 |
|------|------------|
| サブタイトル | 「Phase 9 — LINE WORKS Notification 接続 · 書き込みなし」 |
| LINE WORKS通知 | ロードマップで「実装済」に変更 |
| バージョン | 「v0.9.0 Phase 9」 |
| Provider健全性 | LINE WORKS 通知: 「デモ接続」（ティール色） |

---

## Phase 5.1 変更画面

### 1. 設定画面（Settings.tsx）— 接続前チェックパネル追加

**変更内容**: Google 接続カード（未接続時）に「🔍 接続前チェック」パネルを追加、バージョン v0.5.1 表示

**確認すべき箇所**（未接続かつ `.env` 未設定時）:

| 箇所 | 期待する表示 |
|------|------------|
| 接続前チェックパネル | 「🔍 接続前チェック」セクションが書き込み禁止バナーの下に表示される |
| Client ID設定 | ❌ 未設定（赤バッジ） |
| Redirect URI設定 | ✅ http://localhost:5173/（緑バッジ、デフォルト値） |
| スコープ | ✅ gmail.readonly のみ（緑バッジ・常時） |
| 書き込みAPI | ✅ 未実装（緑バッジ・常時） |
| 本番接続準備 | ❌ 未完了（Client ID 未設定のため赤） |
| バージョン表示 | 「AI社長室 v0.5.1 Phase 5.1」 |

**確認すべき箇所**（`.env` に `VITE_GOOGLE_CLIENT_ID` 設定済み時）:

| 箇所 | 期待する表示 |
|------|------------|
| Client ID設定 | ✅ ****XXXXXXXX（末尾8文字のみ、緑バッジ） |
| Redirect URI設定 | ✅ 設定済みURI（緑バッジ） |
| 本番接続準備 | ✅ 完了（緑バッジ） |

---

## Phase 5 変更画面

### 1. 設定画面（Settings.tsx）— 主要変更

**変更内容**: Google 接続カード追加、バージョン v0.5.0 表示

**確認すべき箇所**:

| 箇所 | 期待する表示 |
|------|------------|
| セクションラベル | 「Google アカウント連携（Phase 5）」 |
| カード上部ステータス | 「□ 未接続」（トークンなし時） |
| 権限セクション | 「○ Gmail 読み取り専用」（グレー、未取得） |
| カレンダー等 | 「— calendar.readonly（Phase 6以降）」 |
| 書き込み禁止バナー | 「⚠️ 書き込み禁止 — 送信・返信・下書き・削除…」 |
| 接続ボタン | 「🔐 Googleアカウントで接続（Gmail ReadOnly）」 |
| 認証ログ | 初回は非表示（ログがない場合） |
| バージョン表示 | 「AI社長室 v0.5.0 Phase 5」 |

**接続後の表示** （`VITE_GOOGLE_CLIENT_ID` 設定・認証成功後）:

| 箇所 | 期待する表示 |
|------|------------|
| ステータスバッジ | 「✓ 接続済み」（緑色） |
| メールアドレス | 接続したGoogle アカウントのアドレス |
| 権限セクション | 「✓ Gmail 読み取り専用」（緑色） |
| ボタン | 「🔄 状態を更新」「🔌 切断する」 |
| 認証ログ | 「▼ 認証ログを見る」が表示される |

### 2. App.tsx — OAuth コールバック処理

**変更内容**: Google からのリダイレクト（`?code=...&state=...`）を自動検知

**確認すべき箇所**:

| 状況 | 期待する動作 |
|------|------------|
| 通常起動 | `?code` なし → 通常ホーム画面 |
| OAuth 成功後リダイレクト | `?code=...&state=...` → 設定画面に遷移、「接続済み」表示 |
| OAuth エラーリダイレクト | `?error=access_denied` → 設定画面に遷移、エラー表示 |

---

## 全画面一覧（Phase 5 時点）

| 画面 | パス（Screen型） | 最終更新 | Phase 5 変更 |
|------|---------------|---------|------------|
| ホーム | `home` | Phase 4.1 | なし |
| AIコックピット | `cockpit` | Phase 4.1 | なし（本番接続後はリアルデータ表示） |
| AI相談チャット | `chat` | Phase 4 | なし |
| 今日の要対応 | `actions` | Phase 4.1 | なし（本番接続後はリアルGmailデータ） |
| 作成依頼 | `create` | Phase 1 | なし |
| 経営ダッシュボード | `dashboard` | Phase 2 | なし |
| **設定** | `settings` | **Phase 5** | **Google接続カード追加** |

---

## 各画面の表示データソース（Phase 5 時点）

| 画面 | データソース |
|------|------------|
| ホーム | `mockData.ts`（todayBriefing・situationCards）+ `mockGmailMessages`（Gmail要対応サマリー） |
| AIコックピット | `mockData.ts` + `mockGmailMessages`（Gmailセクション） |
| AI相談 | `mockData.ts`（キーワード応答） |
| 今日の要対応 | `mockData.ts`（actionItems）+ `mockGmailMessages`（Gmailタスク） |
| 設定 | `mockData.ts`（companies・integrations）+ **`googleSession`**（OAuth状態） |

> **本番接続後**: `mockGmailMessages` が実際の Gmail API データに置き換わる  
> （`gmailClient.ts` の `fetchRawMessages()` が自動的に切り替える）

---

## 画面別想定表示内容チェック

### ホーム画面
- [ ] DemoBanner「⚠️ 現在はデモデータ表示中」
- [ ] Gmail要対応サマリーカード（5件表示・6種チップ）
- [ ] AIブリーフィングカード（展開/折りたたみ可能）
- [ ] クイックアクション 4枚（AI相談・要対応・作成・経営）
- [ ] 今日の状況 8枚グリッド
- [ ] 業務ポータル 4件

### 設定画面（Phase 5 追加箇所）
- [ ] セクション「Google アカウント連携（Phase 5）」
- [ ] ステータス「□ 未接続」（グレー）
- [ ] 権限「○ Gmail 読み取り専用」（グレー）
- [ ] 書き込み禁止バナー（黄色背景）
- [ ] 接続ボタン（ネイビー背景）
- [ ] カレンダー等は「Phase 6以降」表示
