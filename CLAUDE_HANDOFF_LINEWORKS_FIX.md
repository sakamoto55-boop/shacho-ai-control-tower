# Claude Code 引き継ぎ指示書
## LINE WORKSチャット読み込み精度改善

リポジトリ: sakamoto55-boop/shacho-ai-control-tower
対象ブランチ: claude/email-alert-automation-t4xlq3 または新規ブランチ lineworks-accuracy-fix

---

## 背景と問題

現在のLINE WORKSチャット自動化には以下の精度問題があります。

1. **送信者・受信者の誤認識**: 「社長（自分）が送った言葉」を「相手から受け取った言葉」として誤って解釈する。これにより、不要なタスクや返信下書きが生成される。

2. **メッセージ取得の不安定さ**: ブラウザのDOM構造に依存した取得方法は、LINE WORKSのUI変更や動的読み込みに対して脆弱。

3. **AIの判定精度の低さ**: 建設業・不動産業特有の文脈（現場用語、業種特有の緊急度）をAIが正確に判定できていない。

---

## 改善指示（優先順位順）

### 優先度1: `src/domain/types.ts` に会話ログ型を追加

`AnalyzeMessageInput` に `chatHistory` フィールドを追加する。

```typescript
export interface ChatMessage {
  senderType: 'president' | 'other';
  senderName: string;
  text: string;
  timestamp: string;
}

// AnalyzeMessageInput に以下を追加
chatHistory?: ChatMessage[];
```

---

### 優先度2: AIプロンプトの修正（送信者区別の徹底）

`AnthropicProvider.ts` と `OpenAIProvider.ts` のシステムプロンプトを以下の内容に修正する。

**追加すべき指示文（日本語で記述すること）:**

```
あなたは建設会社の代表取締役（社長）を補佐するAIアシスタントです。

【重要ルール】
1. chatHistoryが提供される場合、senderType="president"の発言は「社長が送った言葉」です。これは背景情報として扱い、タスクや返信下書きの対象にしないでください。
2. senderType="other"の発言のみを、社長への連絡として分析してください。
3. 会話の最後の発言者が社長（president）の場合、相手への返信は不要と判断してください。
4. 会話の最後の発言者が相手（other）の場合、返信が必要かどうかを内容で判断してください。

【業種コンテキスト】
この会社は建設・解体・外構・不動産・福祉の5事業を展開しています。
以下の用語や状況は業務上重要です：
- 「現場」「配置」「職人」「資材」「工期」= 建設・外構・解体現場の話
- 「見積」「予算」「発注」= 受注・仕入れに関わる重要案件
- 「クレーム」「騒音」「近隣」= 即時対応が必要なリスク
- 「入居」「退去」「管理費」= 不動産管理の話
- 「利用者」「ケア」「施設」= 福祉事業の話
```

---

### 優先度3: `MockAIProvider.ts` の精度改善

テストが実際の業務に近い精度で動くよう、以下を実装する。

1. `chatHistory` の最後の発言者が `president` の場合、`replyNeeded: false` を返す。
2. テキストに「至急」「クレーム」「騒音」「事故」が含まれる場合、`priority: 'A'` を返す。
3. テキストに「見積」「予算」「確認」が含まれる場合、`priority: 'B'` を返す。
4. 完了報告・相槌（「了解」「ありがとう」「確認しました」）は `priority: 'C'`、`replyNeeded: false` を返す。

---

### 優先度4: `tests/fixtures/messages.ts` にサンプルメッセージを追加

以下のカテゴリのサンプルメッセージを各3件以上追加する。

**建設現場:**
- 「〇〇現場ですが、コンクリートの打設が明日に延期になりました」
- 「現場の職人が足りません。明日2名追加できますか？」

**解体・外構:**
- 「解体現場で近隣から騒音クレームが来ています。至急対応お願いします」
- 「外構工事の資材が届いていません。発注確認をお願いします」

**不動産:**
- 「〇〇物件の入居者から水漏れの連絡が来ました」
- 「退去立会いの日程を確認させてください」

**福祉:**
- 「利用者の〇〇さんが体調不良で本日欠席です」
- 「ケアプランの更新期限が今月末です」

**社長から相手への発言（返信不要パターン）:**
- `chatHistory` の最後が `senderType: 'president'` のケースを必ず含める

---

### 優先度5: 朝昼晩レポートの文面改善

`src/reports/generateDailyReport.ts` と `src/reports/formatLineworksReport.ts` の出力フォーマットを以下に変更する。

**現在の問題**: 箇条書きの羅列で、どれを先に見ればいいか分からない。

**改善後のフォーマット（プレーンテキスト、コピペしやすい形式）:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【朝のレポート】2026/06/13 08:00
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

■ A（即時対応）2件
1. 川野真紀 / スケジュール管理
   → 6/15 定期総会の参加可否を返信してください
   返信下書き: 「参加します。議題があれば事前に共有ください」

2. 成相健一 / 見積もり
   → 予算確認の報告期限が本日中です

■ B（本日中）1件
1. 坂本百合子 / 個人チャット
   → 未返信が続いています。確認してください

■ C（確認のみ・返信不要）3件
・藤井信男: 指示済み案件の進捗報告
・お結び運営: 6/16 MTGの日程確認済み
・見積もり申請: 「全て進めてください」に対する了解返信

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

### 優先度6: テストケースの追加

以下のテストを `tests/ai/analyzeMessage.test.ts` に追加する。

1. **送信者誤認識テスト**: `chatHistory` の最後が `president` の場合、`replyNeeded: false` であることを確認。
2. **建設業緊急度テスト**: 「クレーム」「至急」を含むメッセージが `priority: 'A'` になることを確認。
3. **完了報告テスト**: 「了解しました」のみのメッセージが `priority: 'C'`、`replyNeeded: false` になることを確認。

---

## 制約（変更しないこと）

- 個人LINEの直接連携は作らない
- AIによる外部自動返信は作らない
- 金額・契約・納期・謝罪・責任認定は自動確定しない
- APIキーをコードに直書きしない
- 扱うsourceは gmail, lineworks, manual_import, external_forward のみ

---

## 完了後にすること

1. `npm test` で全テストがpassすることを確認
2. `npm run build` でビルドエラーがないことを確認
3. 変更内容をコミット・プッシュ
4. 引き継ぎレポートを更新して社長に報告
