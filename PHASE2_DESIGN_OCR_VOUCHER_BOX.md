# フェーズ2設計書：OCR連携・証憑BOX

## 概要

本文書は、LCC統合業務アプリの**フェーズ2**機能として計画している以下2機能の設計書です。

- **証憑BOX**：紙の請求書・領収書・納品書等を写真で撮影してクラウド保存・案件連携
- **OCR連携**：段階的OCR導入による文字抽出・請求書構造化

> **重要制約**
> - フェーズ2機能は本体への即時実装ではなく、設計・計画段階とする
> - OCR系APIは本書承認なしに有効化禁止
> - 新規費用・月額サブスク・従量課金は別途費用見積・月間想定枚数・上限設定・承認が必要

---

## 1. 証憑BOX（Voucher Box）

### 1.1 目的

紙の請求書・領収書・納品書・伝票・処分場伝票・マニフェスト関連資料等を  
スマホで撮影し、クラウド保存して、案件・原価・支払いに紐付ける。

### 1.2 データモデル

```typescript
interface Voucher {
  id: string;                    // 伝票ID（uuid）
  projectId: string;             // 案件ID（案件1件に複数伝票）
  vendorId: string;              // 協力会社ID
  fileId: string;                // ファイルID（画像/PDF）
  fileUrl: string;               // ストレージURL
  thumbnailUrl: string;          // サムネイルURL

  documentType: VoucherDocType;  // 書類種別
  transactionDate: string;       // 取引日（ISO 8601）
  vendorName: string;            // 取引先名（手入力 or OCR）
  amount: number;                // 金額（税込）
  taxAmount: number;             // 税額
  costCategory: string;          // 費目

  paymentDueDate: string;        // 支払予定日
  checkStatus: CheckStatus;      // 確認ステータス

  ocrRawText: string;            // OCR読取結果（生テキスト）
  ocrStructured: OcrResult|null; // OCR構造化結果
  humanVerified: boolean;        // 人による確認済みフラグ
  costApplied: boolean;          // 原価反映フラグ
  paymentApplied: boolean;       // 支払反映フラグ

  memo: string;
  createdAt: number;
  updatedAt: number;
}

type VoucherDocType =
  | '請求書'
  | '領収書'
  | '納品書'
  | '見積書（外注）'
  | '処分場伝票'
  | 'マニフェスト'
  | 'その他';

type CheckStatus =
  | '未確認'
  | '確認中'
  | '確認済み'
  | '差戻し';

interface OcrResult {
  invoiceDate: string;
  vendorName: string;
  totalAmount: number;
  taxAmount: number;
  items: Array<{ description: string; amount: number }>;
  confidence: number; // 0.0-1.0
}
```

### 1.3 UI画面設計

#### 証憑BOX一覧画面

```
┌────────────────────────────────────┐
│ 証憑BOX                   ＋ 追加 │
├────────────────────────────────────┤
│ [案件で絞込▾] [種別▾] [確認状態▾] │
├────────────────────────────────────┤
│ 📄 ○○建材㈱　請求書        未確認 │
│    案件：出雲市〇〇様邸            │
│    2026/06/15　¥128,700            │
├────────────────────────────────────┤
│ 📄 □□産廃㈱　処分場伝票   確認済み│
│    案件：出雲市〇〇様邸            │
│    2026/06/14　¥45,000             │
└────────────────────────────────────┘
```

#### 伝票登録フォーム

```
┌────────────────────────────────────┐
│ 伝票を登録               保存      │
├────────────────────────────────────┤
│ 📷 写真を撮影 or PDF選択           │
│ ┌──────────────────────────┐       │
│ │    [写真プレビュー]       │       │
│ └──────────────────────────┘       │
│ 書類種別: [請求書 ▾]               │
│ 案件: [出雲市〇〇様邸 ▾]           │
│ 取引先: ________________________   │
│ 取引日: [2026-06-15]               │
│ 金額: ¥ ____________________       │
│ 税額: ¥ ____________________       │
│ 費目: [材料費 ▾]                   │
│ 支払予定: [2026-07-31]             │
│ メモ: ________________________     │
└────────────────────────────────────┘
```

---

## 2. OCR導入方針（段階的）

### フェーズ2-1（現在の設計対象）：OCRなし版

**期間目標**：フェーズ1 UAT完了後、即実装可能

| 機能 | 内容 |
|------|------|
| 写真・PDF保存 | スマホカメラで撮影してクラウド保存 |
| 手入力 | 取引先・金額・日付を手入力 |
| 案件連携 | 案件IDに紐付け |
| 一覧管理 | 書類種別・確認状態でフィルタ |
| 支払管理 | 支払予定日・完了フラグ |

**費用**：クラウドストレージのみ（Google Drive APIまたはFirebase Storage相当）  
**月額目安**：月100枚以下なら無料枠内

---

### フェーズ2-2：テキスト抽出試験導入

**期間目標**：フェーズ2-1稼働後3ヶ月以内

| API | 用途 | 費用 |
|-----|------|------|
| Google Drive OCR | PDFをテキスト変換（Drive保存時に自動） | 無料（Drive内） |
| Cloud Vision API | 画像からテキスト抽出 | **要有効化・従量課金** |

**注意**：  
- Cloud Vision APIは**勝手に有効化しない**  
- 月間想定枚数・上限設定・承認後に有効化  
- 1枚あたり約$0.0015（1000枚=$1.50）

---

### フェーズ2-3：請求書構造化

**期間目標**：フェーズ2-2の精度検証後

| API | 用途 | 費用 |
|-----|------|------|
| Document AI Invoice Parser | 請求日・取引先・金額・税額・明細を構造化 | **要有効化・従量課金** |

**注意**：  
- 月1000件まで無料枠あり（超過：$0.065/件）  
- **本番有効化前に費用見積・承認が必要**

---

### フェーズ2-4：OCR結果の確認・反映ワークフロー

```
写真撮影
   ↓
OCR自動抽出（Cloud Vision / Document AI）
   ↓
抽出結果を画面表示（編集可能）
   ↓
[人による確認・修正]  ←← 必ずここを通る
   ↓
「確認済み」フラグON
   ↓
原価反映（手動ボタン）
支払反映（手動ボタン）
```

**原則**：  
- OCR結果は**そのまま原価・支払に反映しない**  
- 必ず人が確認してから「原価に反映」「支払に反映」ボタンを押す  
- 金額・取引先の自動確定は禁止

---

## 3. OCR導入時の費用リスク表

| 項目 | フェーズ2-1 | フェーズ2-2 | フェーズ2-3 | フェーズ2-4 |
|------|-------------|-------------|-------------|-------------|
| Cloud Vision API | 不要 | **要有効化** | 不要（DAI使用） | 継続 |
| Document AI | 不要 | 不要 | **要有効化** | 継続 |
| 月間想定枚数 | - | 50〜200枚 | 50〜200枚 | 同左 |
| 月額費用（50枚） | ¥0 | 約¥11 | 約¥0（無料枠内） | 約¥11 |
| 月額費用（200枚） | ¥0 | 約¥44 | 約¥0（無料枠内） | 約¥44 |
| 月額費用（1000枚） | ¥0 | 約¥220 | 約¥0（無料枠内） | 約¥220 |
| 月額費用（5000枚） | ¥0 | 約¥1,100 | 約¥29,000 | 約¥30,100 |
| 上限設定 | 不要 | **必須** | **必須** | **必須** |
| 承認必要 | 不要 | **要** | **要** | 不要（継続） |

**リスク対策**：
- 上限設定（Budget Alert）を必ず設定する
- 上限超過でAPIを自動停止するアラートを設定する
- 月次でAPI使用料をモニタリングする
- 大量スキャン利用が見込まれる場合は事前承認を得る

---

## 4. 本線UAT環境への反映可否

| 機能 | 本線UAT反映 | 理由 |
|------|------------|------|
| 必須改修1〜7（本体修正） | **可** | バグ修正・業務導線改善のみ |
| 証憑BOX（フェーズ2-1） | **保留** | UAT完了後に判断 |
| OCR連携（フェーズ2-2〜4） | **不可（現時点）** | 費用承認・技術検証が必要 |

---

## 5. ロールバック手順

### 必須改修1〜7（本体修正）のロールバック

```bash
# Gitログで修正前コミットを確認
git log --oneline lcc-redesigned.html

# 修正前バージョンに戻す
git checkout <修正前コミットハッシュ> -- lcc-redesigned.html

# ブラウザでlocalStorageをクリアして再起動
# DevTools > Application > Local Storage > lcc_v1 > 右クリック削除
```

### バックアップからの復元

アプリ内の「設定 > バックアップ・復元 > バックアップ書き出し」で  
`.json`ファイルをローカルに保存しておくこと。

復元時は「バックアップから復元」ボタンで読み込む。

---

## 更新履歴

| 日付 | 内容 |
|------|------|
| 2026-06-28 | フェーズ2設計書初版作成（claude/lcc-data-persistence-fix） |
